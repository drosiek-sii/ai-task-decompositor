{
  "name": "agent-beads",
  "version": "0.1.0",
  "description": "CLI that turns prompts and .md plans into JIRA-style tasks, validates them via the codex-brainstorm skill, and writes them to Beads.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "agent-beads": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"if(process.platform!=='win32')require('fs').chmodSync('dist/cli.js',0o755)\"",
    "dev": "tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
